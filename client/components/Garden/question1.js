import React from 'react'

const Question1 = () => {
  return (
    <div>
      <label>Where is your garden?</label>
      <select>
        {/* {props.distributionZones.map((zone, index) => {
          return <option key={index}>{zone.name}</option>
        })} */}
      </select>
    </div>
  )
}

export default Question1
